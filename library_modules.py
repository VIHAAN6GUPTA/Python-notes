from lib.dog import bark

bark()

# Modules

# Import the math module for mathematical functions like sqrt, sin, cos, etc.
import math

# Import the re module for working with Regular Expressions (pattern matching in text)
import re

# Import the json module to encode and decode JSON data (useful for APIs and config files)
import json

# Import the datetime module to manipulate dates and times
import datetime

# Import the sqlite3 module to interact with SQLite databases (lightweight, file-based databases)
import sqlite3

# Import the os module to interact with the Operating System (e.g., file paths, environment variables)
import os

# Import the random module to generate random numbers and make random choices
import random

# Import the statistics module to perform statistical calculations like mean, median, etc.
import statistics

# Import the requests module to make HTTP requests (GET, POST, etc.) – great for APIs
import requests

# Import the http module to build and work with HTTP servers (basic web server functionality)
import http

# Import the urllib module to handle and parse URLs, and perform basic HTTP operations
import urllib
